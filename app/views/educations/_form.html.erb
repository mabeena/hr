<%= form_for(@education) do |f| %>  
    <%= f.select :degree, Education::ED_TYPES %>
    <%= f.text_field :concentration, :placeholder => "concentration" %>  
    <%= f.submit "Add Education" %>
<% end %>
